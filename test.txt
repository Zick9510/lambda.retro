, Examples

(\x . x);

\x y . y;

(\x . \y . x); , Lambda function

(\x    y . x); , Syntactic sugar for multiple-arguments lambda functions

, This is a comment

(\i j k. (j k) (i i) ) ,This is also a comment, 1 2 3; , Expressions are semicolon-terminated

(\x y. x) 1; , Lambda function. One argument passed

(\x y. x) 1 2; , Two args passed

(lambda f . f ) 1; , Still a Lambda function. Using the word instead of the symbol

func R ( f ) { f } , Named function declaration

func A ( f g ) { g f }

func B ( f g ) { (A f g) (A f g) }

B 3 4; , Function call

R 1;

add 1 2;
sub 3 2;
mult 4 (add 3 2);

func SUCC (n f x) { f ( n f x ) }

func Z ( f ) { (\x . f (\v . x x v)) (\x . f (\v . x x v)) }

func TRUE  ( x y ) { x }
func FALSE ( x y ) { y }

func PAIR ( a b f ) { f a b   }
func FIRST      (p) { p TRUE  }
func SECOND     (p) { p FALSE }

func SHIFT (p) { PAIR (SECOND p) ( SUCC (SECOND p)) }

func PRED (n) { FIRST (n SHIFT (PAIR 0 0)) }

func SUB ( a b f x ) { b PRED a f x }

func ADD ( a b f x ) { a SUCC b f x }

func MULT ( a b f x ) { a (b f) x }

func IS_0 ( n ) { n (\x . FALSE) TRUE }

func AND (a b) { a b FALSE }

func NOT (a) { a FALSE TRUE }

func OR (a b) { NOT (AND (NOT a) (NOT b)) }

func XOR (a b) { AND (OR a b) (NOT (AND a b)) }

func EQ (a b) { AND (IS_0 (SUB a b)) (IS_0 (SUB b a)) }

ADD SUB 5 3 SUB 5 1; , Dear reader: Compare this result

ADD (SUB 5 3) (SUB 5 1); , With this one

func FACT_GEN ( recurse n ) { , I am sorry for my bad code
(
	(
		(
			( IS_0 n )
			( \d . 1 )
		)
		( \d .
			(
				( MULT n )
				( recurse ( SUB n 1 ) )
			)
		)
	) 0 , Dummy value. Can be anything
)
}

,func FACT ( n ) { (( Z FACT_GEN ) n ) } ,, Two ways of implementing recursion
func FACT ( n ) { fix FACT_GEN n }

FACT 3;

func FIB_GEN ( recurse n ) {
( ( IS_0 n )
	( \d . 0 )
	( \d .
	( IS_0 ( SUB n 1 )
			( \d2 . 1 )
			( \d2 .
				ADD ( recurse ( SUB n 1 ) ) ( recurse ( SUB n 2 ) )
			)
		) 0
	)
) 0
}

func FIB ( n ) { fix FIB_GEN n }

func + ( a b f x ) { ADD a b f x }

func - ( a b f x ) { SUB a b f x }

EQ (ADD 1 2) (+ 1 2);

func POWER ( a b f x ) { b a f x } , a ** b

POWER 2 3;

func +14vb/%&%/%a/&aa23t ( a b ) { b a } , Still a named function

+14vb/%&%/%a/&aa23t 2 3;

POWER 3 3;

func LT (a b) { AND (IS_0 (SUB a b)) (NOT (IS_0 (SUB b a))) }

func DIV_GEN ( recurse n m ) {
(
	( LT n m )
	( \d . 0 )
	( \d .
		SUCC ( recurse ( SUB n m ) m )
	) 0
)
}

func DIV ( n m ) {
( IS_0 m )
( \d . 0 )
( \d .
	fix DIV_GEN n m
) 0
}

func EMPTY ( f ) { TRUE }
func IS_EMPTY ( l ) { l (\x y. FALSE)  }

func SUM_LIST_GEN (recurse l) {
(
	( IS_EMPTY l)
	( \d . 0)
	( \d .
		ADD
			(FIRST l)
			(recurse (SECOND l))
	)
) 0
}

func SUM_LIST ( l ) { fix SUM_LIST_GEN l }

SUM_LIST ( PAIR 2 ( PAIR 7 ( PAIR 1 EMPTY ) ) );

SUM_LIST [ PAIR | 3 1 4 EMPTY ];

, Maps: Functional Approach

func EMPTY_MAP ( k ) { EMPTY }

func INSERT ( M key value ) {
\query_key . (EQ query_key key) (value) (M query_key)
}

(INSERT (INSERT EMPTY_MAP 1 2) 2 3) 1;
(INSERT (INSERT EMPTY_MAP 1 2) 2 3) 2;
(INSERT (INSERT EMPTY_MAP 1 2) 2 3) 3;

func M1 ( k ) { INSERT EMPTY_MAP 1 2 k }
func M2 ( k ) { INSERT M1 2 3 k }
func M3 ( k ) { INSERT M2 3 4 k }
func M4 ( k ) { INSERT M3 2 EMPTY k }

M4 1;

M2 2;
M4 2;

, Maps: Linked-lists Approach

func HEAD ( p ) { p TRUE  }
func TAIL ( p ) { p FALSE }

func GET_REC ( self M key ) { , Presonally I prefer this style of recursion
( ( IS_EMPTY M )
	( EMPTY )
	( ( EQ ( HEAD ( HEAD M ) ) key)
		( \d . ( TAIL ( HEAD M ) ) ) , This is called a "thunk". No idea what it means,, I call it a dummy lambda function. Prevents the engine's eager evaluation
		( \d . ( self self ( TAIL M ) key ) ), If you don't have dummies,, the engine would eval this "self self" into segfault realm (oblivion,, which is bad)
	) 0
)
}

func GET ( M k ) { GET_REC GET_REC (M 0) k }

func DEL_REC ( self M key ) {
( IS_EMPTY M )
	( EMPTY )
	( (EQ (HEAD (HEAD M)) key )
		( \d . (self self (TAIL M) key ))
		( \d . PAIR (HEAD M) (self self (TAIL M) key))
	) 0
}

func DELETE ( M k ) { \d . DEL_REC DEL_REC (M 0) k }

func INSERT ( M k v ) { \d . PAIR ( PAIR k v ) (M d) }

func Mx ( d ) {
	{ PAIR |
		1 : (PAIR 1 2)
		2 : (PAIR 4 8)
		EMPTY
	}
}

Mx;

GET Mx 2;
DELETE Mx 2;
GET (INSERT Mx 3 (PAIR 9 18)) 2;
GET (INSERT Mx 3 (PAIR 9 18)) 3;

func My ( d ) { INSERT Mx 3 (PAIR 9 18) d }

Mx; My;

GET My 2;
GET My 3;

GET (DELETE Mx 2) 2;

func Mz ( d ) {
{ PAIR | , Syntactic sugar for maps
	0 :  0
	1 : 10
	2 : 20
	3 : 30
	4 : 40
	EMPTY
}
}

GET Mz 3;

'\0'; '\12'; 'a'; 0b0101; 0o070; 0xffff; , Every single one is a number

0xAA_55; 0b0000_1111; , You might want to use underscore to make numbers more readable

04_423_12_523324_2; , It doesn't matter how. No need to keep order here

func MAP_REC ( self list f ) { , Applies a function to each element on a list
( ( IS_EMPTY list )
		( \d . EMPTY )
		( \d . PAIR (f (HEAD list)) (self self (TAIL list) f )) , List not empty: apply function to the first element,, recursive call on the second
) 0
}

func MAP ( list f ) { MAP_REC MAP_REC list f }

func FILTER_REC ( self f list ) {
( ( IS_EMPTY list )
	( \d . EMPTY )
	( \d .
		( ( f (HEAD list) )
			( \d2 .  PAIR ( HEAD list ) ( self self f (TAIL list) ))
			( \d2 . self self f (TAIL list) )
		) 0
	)
) 0
}

func FILTER ( list f ) { FILTER_REC FILTER_REC f list }

func +1 ( a ) { + a 1 }

(PAIR 1 (PAIR 2 (PAIR 3 (PAIR 4 EMPTY)))); , Linked list

[ PAIR | 1 2 3 4 EMPTY ]; , Syntactic sugar for linked lists

MAP [ PAIR | 1 2 3 4 EMPTY ] +1;

FILTER [ PAIR | 1 2 2 3 2 1 2 3 2 3 EMPTY ] (EQ 2);

[ ADD | 2 3 4 5 6 ];

func FOLDR_REC ( self f acc list ) {
( ( IS_EMPTY list )
	( \d . acc )
	( \d . f ( HEAD list ) ( self self f acc ( TAIL list ) ) )
) 0
}

func FOLDR ( f acc list ) { FOLDR_REC FOLDR_REC f acc list }

func FOLDL_REC ( self f acc list ) {
( ( IS_EMPTY list )
	( \d . acc )
	( \d . self self f ( f acc ( HEAD list ) ) ( TAIL list ) )
) 0
}

func FOLDL ( f acc list ) { FOLDL_REC FOLDL_REC f acc list }

func INC_ACC ( acc x ) { + acc 1 }
func LENGTH ( list ) { FOLDL INC_ACC 0 list }

LENGTH [ PAIR | 1 2 3 4 5 0 2 3 EMPTY ];

func APPEND_REC ( self l1 l2 ) {
( ( IS_EMPTY l1 )
		( \d . l2 )
		( \d . PAIR ( HEAD l1) (self self (TAIL l1) l2 ))
) 0
}

func APPEND ( l1 l2 ) { APPEND_REC APPEND_REC l1 l2 }

APPEND [ PAIR | 1 2 3 EMPTY ] [ PAIR | 4 5 6 EMPTY ];

func ANY_REC ( self f list ) {
( ( IS_EMPTY list )
		( \d . FALSE )
		( \d .
			( ( f ( HEAD list ) )
							( \d2 . TRUE )
							( \d2 . self self f ( TAIL list ))
			) 0
		)
) 0
}

func ANY ( f list ) { ANY_REC ANY_REC f list }

func INDEX_REC ( self list i ) {
( ( IS_EMPTY list )
		( \d . EMPTY )
		( \d .
			( ( EQ i 0 )
				( \d2 . HEAD list )
				( \d2 . self self ( TAIL list ) ( SUB i 1 ) )
			) 0
		)
) 0
}

func INDEX ( list i ) { INDEX_REC INDEX_REC list i }

func REVERSE_STEP ( acc x ) { PAIR x acc }
func REVERSE ( list ) { FOLDL REVERSE_STEP EMPTY list }

func TAKE_REC ( self list n ) {
( ( IS_EMPTY list )
		( \d . EMPTY )
		( \d .
			( ( EQ n 0 )
				( \d2 . EMPTY )
				( \d2 . PAIR ( HEAD list ) (self self ( TAIL list ) ( SUB n 1 ) ) )
			) 0
		)
) 0
}

func TAKE ( list n ) { TAKE_REC TAKE_REC list n }

func DROP_REC ( self list n ) {
( ( IS_EMPTY list )
		( \d . EMPTY )
		( \d .
			( ( EQ n 0 )
				( \d2 . list )
				( \d2 . self self ( TAIL list ) ( SUB n 1 ) )
			) 0
		)
) 0
}

func DROP ( list n ) { DROP_REC DROP_REC list n }

func SLICE ( list a b ) { TAKE ( DROP list a ) b }

func SET_REC ( self list i v ) {
	( ( IS_EMPTY list )
			( \d . EMPTY )
			( \d .
				( ( EQ i 0 )
						( \d2 . PAIR v ( TAIL list ) )
						( \d2 . PAIR ( HEAD list ) ( self self ( TAIL list ) ( SUB i 1 ) v ) )
				) 0
			)
	) 0
}

func SET ( list i v ) { SET_REC SET_REC list i v }

func INSERT_AT_REC ( self list i v ) {
	( ( IS_EMPTY list )
			( \d . ( ( EQ i 0 ) ( \d2 . PAIR v EMPTY ) ( \d2 . EMPTY ) ) 0 )
			( \d .
				( ( EQ i 0 )
					( \d2 . PAIR v list )
					( \d2 . PAIR ( HEAD list ) ( self self ( TAIL list ) ( SUB i 1 ) v ) )
				) 0
			)
	) 0
}

func INSERT_AT ( list i v ) { INSERT_AT_REC INSERT_AT_REC list i v }

func REMOVE_REC ( self list i ) {
	( ( IS_EMPTY list )
			( \d . EMPTY )
			( \d .
				( ( EQ i 0 )
					( \d2 . TAIL list )
					( \d2 . PAIR ( HEAD list ) ( self self ( TAIL list ) ( SUB i 1 ) ) )
				) 0
			)
	) 0
}

func REMOVE ( list i ) { REMOVE_REC REMOVE_REC list i }

func +2 ( a ) { ADD a 2 }

func *3 ( a ) { MULT a 3 }

*3 (+2 1);

*3 . +2 1; , Function composition // f . g . h . x -> (f ( g ( h x ) ) )

MAP [ PAIR | 1 (2 3) 3 4 EMPTY ] +1;

MAP (MAP [ PAIR | 1 2 3 4 EMPTY ] +1) *3;

[ PAIR | 1 2 3 4 EMPTY ] | MAP +1 | MAP *3; , previous expression servers as the following's first argument

[ PAIR | 1 2 3 4 EMPTY ] | MAP FACT . +1;

[ PAIR | "Howdy" EMPTY ];

import "test2.txt";

SWAP 2 3;
